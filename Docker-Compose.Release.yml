version: "3.7"
services:
    application:
        build:
            image: ${CONTAINER_REGISTRY}:application
    nginx:
        build:
            image: ${CONTAINER_REGISTRY}:nginx
        ports:
            - "80:80"
            - "443:443"